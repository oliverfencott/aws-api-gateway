{
  "name": "@serverless/aws-api-gateway",
  "version": "4.1.0",
  "main": "./serverless.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "serverless registry publish --dev",
    "deploy": "cd test-components && serverless deploy --all --debug && cd ..",
    "remove": "cd test-components && serverless remove --all --debug && cd ..",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint . --fix --cache"
  },
  "author": "Serverless, Inc.",
  "license": "Apache",
  "dependencies": {
    "@serverless/core": "^1.0.0",
    "@serverless/platform-client": "^0.25.7",
    "aws-sdk": "^2.0.0",
    "p-retry": "^4.1.0"
  },
  "devDependencies": {
    "babel-eslint": "9.0.0",
    "eslint": "5.6.0",
    "eslint-config-prettier": "^3.6.0",
    "eslint-plugin-import": "^2.14.0",
    "eslint-plugin-prettier": "^3.0.1",
    "prettier": "^1.15.3"
  }
}